- schema:
  - bin: (some integrated shell (or may be not only) binaries)
    - callable
    - [ex] -- currently missing
    - git_current_branch
    - lscmd
    - mute
    - peek
    - serve
    - tmux_status
    - wd -- Warp directory.

  - configs: (to symlink into home dir)
    - .dircolors -- ls(??) color config.
    - .gemrc     -- rubygems config (currently seems to broken)
    - .minittyrc -- minitty (cygwin??) config.
    - .nanorc    -- nano config.
    - .tmux.conf -- tmux config.
    - .zshrc     -- main dotfiles entrypoint.

  - lib: (zsh main config)
    - colors
    - completition
    - functions
    - history
    - zle
  - strains: ()
  - traits: ()
  -- bootsrap.zsh
  -- setup.zsh
