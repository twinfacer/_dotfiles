# Warp anyware, local or remote - warp does not care.
# Usage:
# $ warp vip
# => Warping to ~/projects/vip-client-app
# $ warp @prod3
# => Warping to 192.168.001.013 as it_stuff
# $ warp idk
# => Unknown warp point =(
# => Try add some by `warp -a <name> [<dir> || .]` to add <dir> or current directory if <dir> is empty
# Or `warp -a <user>@<host_or_ip>:[<ssh_port> || 22]`
# => Unknown warp point =(
