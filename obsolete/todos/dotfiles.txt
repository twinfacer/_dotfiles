- schema:
  - bin: (some integrated shell (or may be not only) binaries)
    - git_current_branch
    - mute
    - peek
    - serve
    - tmux_status
    - wd -- Warp directory. (replace with warp.sh)

  - configs: (to symlink into home dir)
    - .dircolors -- ls(??) color config.
    - .gemrc     -- rubygems config
    - .minittyrc -- minitty (cygwin??) config.
    - .nanorc    -- nano config.
    - .tmux.conf -- tmux config.
    - .zshrc     -- main dotfiles entrypoint.

  - lib: (zsh main config)
    - colors
    - completition (seams broken)
    - functions
    - history
    - zle
  - strains: ()
  - traits: ()
  -- setup.zsh
